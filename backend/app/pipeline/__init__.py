"""Pipeline orchestration package.

Provides async pipeline functions for article generation and publishing:

- :func:`single_article.generate_single_article` — full single-article flow
- :func:`bulk_pipeline.run_bulk_pipeline` — bulk pipeline with concurrent processing
"""
